{
  "name": "subfloor-agent",
  "private": true,
  "type": "module",
  "description": "SUBFLOOR agent host: Ledger Key Ring enrollment and ring-held secrets",
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "keyring": "node --experimental-strip-types src/cli.ts"
  },
  "dependencies": {
    "@ledgerhq/hw-ledger-key-ring-protocol": "0.10.7",
    "@ledgerhq/ledger-key-ring-protocol": "0.15.2",
    "@ledgerhq/speculos-transport": "0.10.6",
    "@ledgerhq/live-dmk-speculos": "file:./vendor/live-dmk-speculos",
    "ws": "8.18.3"
  },
  "overrides": {
    "@ledgerhq/live-dmk-speculos": "file:./vendor/live-dmk-speculos"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.0"
  }
}
