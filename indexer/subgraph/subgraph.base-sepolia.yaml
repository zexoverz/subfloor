# Deploying a new version RETIRES the previous one on Studio, and a fresh version takes roughly ten
# minutes before it processes its first block — it sits at `startBlock - 1` with
# `hasIndexingErrors: false` the whole time, which is indistinguishable from being broken.
#
# So: deploy once and wait. Deploying again to "check" kills the version that was about to start,
# which is how 7 Sep lost two hours to a bug that did not exist.
#
# startBlock is the contracts' creation block, read from broadcast/*/run-latest.json.
specVersion: 1.2.0
description: >
  Base Sepolia build, for frontend integration only. Same schema and handlers as the mainnet
  manifest; the addresses come from DeploySubfloorTestnet.s.sol and the Aqua here is ours rather
  than canonical, because canonical Aqua is not deployed on Sepolia.
repository: https://github.com/zexoverz/subfloor
schema:
  file: ./schema.graphql

dataSources:
  # Canonical Aqua. Note that not one parameter on any of these four events is indexed, so nothing
  # here can be filtered by maker at the log level: every Shipped/Pulled/Pushed/Docked on the venue
  # arrives and is narrowed after decoding. See docs/event-map.md.
  - kind: ethereum
    name: FloorRouter
    network: base-sepolia
    source:
      address: "0xa2C76F6eF597B4E48d98A6085B9381C7b0fa0709"
      abi: FloorRouter
      startBlock: 46513825
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      file: ./src/router.ts
      entities:
        - Swap
        - FillQuality
        - Account
      abis:
        - name: FloorRouter
          file: ./abis/FloorRouter.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: Swapped(bytes32,address,address,address,address,uint256,uint256)
          handler: handleSwapped

  # The registry's events are the one place upstream indexing gets easy: recipient, base and quote
  # are all indexed, so floor history for one recipient is a topic filter rather than a scan.
  - kind: ethereum
    name: FloorRegistry
    network: base-sepolia
    source:
      address: "0x47c7AbB1FfbF37eD4bCFCB20f6648B5c0cC86123"
      abi: FloorRegistry
      startBlock: 46513825
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      file: ./src/registry.ts
      entities:
        - Floor
        - FloorChange
      abis:
        - name: FloorRegistry
          file: ./abis/FloorRegistry.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: FloorRaised(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleFloorRaised
        - event: FloorLowered(indexed address,indexed address,indexed address,uint256,uint256,address)
          handler: handleFloorLowered
        - event: ToleranceTightened(indexed address,indexed address,indexed address,uint16,uint16)
          handler: handleToleranceTightened
        - event: ToleranceWidened(indexed address,indexed address,indexed address,uint16,uint16,address)
          handler: handleToleranceWidened

  # The Chainlink aggregator, not the proxy. The proxy emits nothing; the aggregator behind it does.
  # Read off the proxy with `aggregator()` and proved live by event recency — 13 AnswerUpdated logs
  # in the last 4,000 blocks — rather than by assuming a contract that answers calls is in use.
  - kind: ethereum
    name: ChainlinkEthUsd
    network: base-sepolia
    source:
      address: "0xa24A68DD788e1D7eb4CA517765CFb2b7e217e7a3"
      abi: ChainlinkAggregator
      startBlock: 46513825
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      file: ./src/reference.ts
      entities:
        - ReferenceAnswer
      abis:
        - name: ChainlinkAggregator
          file: ./abis/ChainlinkAggregator.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: AnswerUpdated(indexed int256,indexed uint256,uint256)
          handler: handleAnswerUpdated
