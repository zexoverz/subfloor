node_modules/
out/
cache/
broadcast/
target/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
CLAUDE.local.md
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
docs/DEMO-LIVE.md
.playwright-mcp/
*.png
feedbacks/
# Deliberate frontend assets are not browser artefacts: the hero drawing ships with the page.
!frontend/public/*.png
# The Substreams package image is part of the manifest: without it `substreams pack` fails on a
# fresh clone, and the registry listing goes up with a blank tile.
!indexer/substreams/logo.png
